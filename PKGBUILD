# Maintainer    :   Maurício de Lima (mauriciodelima.mol@gmail.com)

pkgname=uaiso-rdp
pkgver=$(date +%y.%m.%d)
pkgrel=$(date +%H%M)
pkgdesc="Package connect RDP Windows"
arch=('any')
url="https://uaiso.uai21.com/orion"
license=('GPL')

depends=('krdc' 
        'freerdp' 
        'libvncserver'
        'keditbookmarks'
        'anydesk-bin'
        'rustdesk-bin'
        )

#optdepends=()
#install=$pkgname.install
